# /etc/conf.d/botcoind: config file for /etc/init.d/botcoind

# Config file location
#BLACKMORED_CONFIGFILE="/etc/botcoin/botcoin.conf"

# What directory to write pidfile to?  (created and owned by $BLACKMORED_USER)
#BLACKMORED_PIDDIR="/var/run/botcoind"

# What filename to give the pidfile
#BLACKMORED_PIDFILE="${BLACKMORED_PIDDIR}/botcoind.pid"

# Where to write botcoind data (be mindful that the blockchain is large)
#BLACKMORED_DATADIR="/var/lib/botcoind"

# User and group to own botcoind process
#BLACKMORED_USER="botcoin"
#BLACKMORED_GROUP="botcoin"

# Path to botcoind executable
#BLACKMORED_BIN="/usr/bin/botcoind"

# Nice value to run botcoind under
#BLACKMORED_NICE=0

# Additional options (avoid -conf and -datadir, use flags above)
#BLACKMORED_OPTS=""

# The timeout in seconds OpenRC will wait for botcoind to terminate
# after a SIGTERM has been raised.
# Note that this will be mapped as argument to start-stop-daemon's
# '--retry' option, which means you can specify a retry schedule
# here. For more information see man 8 start-stop-daemon.
BLACKMORED_SIGTERM_TIMEOUT=600
